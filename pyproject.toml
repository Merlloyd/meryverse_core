[project]
name = "meryverse-core"
version = "0.4.0"
description = "Shared utilities for Meryverse apps (design tokens, crypto helpers)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["meryverse_core*"]
